# Metadata

Source manifests pin every acquired demo file. Component release manifests pin the processed snapshots used by integration. The integration source manifest pins both upstream manifests and linkage bytes. data_dictionary.csv and field_provenance.csv explain fields. result_index.json maps outputs to commands. Research manifests must add access, licensing, population and version evidence.
