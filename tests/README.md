# Tests

Run python3 -m unittest discover -s tests -v. Tests check known fixture outcomes and stop conditions for corrupt inputs, missing assets, conflicts, ambiguous linkage, future information and unsupported inference. Reference equality is separate from real-world measurement/linkage accuracy.
