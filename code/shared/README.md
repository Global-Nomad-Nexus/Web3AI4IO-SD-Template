# Shared implementation

pipeline.py implements synthetic CSV I/O, source hashes, schema contracts, processing, time-safe demo joins, analysis and reference comparison. Track run.py files are public entry points. Keep project-specific choices documented and avoid copying divergent join logic into notebooks.
