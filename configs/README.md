# Configuration

Copy the example JSON files, replace null author fields with verified facts, and version them with code. Demo constants live in code/shared/pipeline.py. The research configuration is an author worksheet and is not consumed by the demo pipeline.
